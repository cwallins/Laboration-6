import React, { useState, useRef } from 'react';

export default function orderByAlpha() {
    return (
        <div>
            <input type="submit" class="btn btn-primary mt-3" value="Alfabetisk ordning"></input>
        </div>
    )
}